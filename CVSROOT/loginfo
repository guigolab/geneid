# The "loginfo" file controls where "cvs commit" log information
# is sent.  The first entry on a line is a regular expression which must match
# the directory that the change is being made to, relative to the
# $CVSROOT.  If a match is found, then the remainder of the line is a filter
# program that should expect log information on its standard input.
#
# A commit in any project whose name matches a regexp, would run the associated command,
# unless the commit had already matched a previous regexp !!!
#
# If the repository name does not match any of the regular expressions in this
# file, the "DEFAULT" line is used, if it is specified.
#
# If the name ALL appears as a regular expression it is always used
# in addition to the first matching regex or DEFAULT.
#
# You may specify a format string as part of the
# filter.  The string is composed of a `%' followed
# by a single format character, or followed by a set of format
# characters surrounded by `{' and `}' as separators.  The format
# characters are:
#
#   s = file name
#   V = old version number (pre-checkin)
#   v = new version number (post-checkin)
#
CVSROOT  (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.CVSROOT
CVSROOT  /usr/bin/Mail -s "CVS update report: CVSROOT has been updated" gmaster@imim.es
#
# 
software   (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.software
# gfftools /usr/bin/Mail -s "CVS update report: GFFTOOLS project" jabril@imim.es
# sgp      /usr/bin/Mail -s "CVS update report: SGP project" jabril@imim.es
# sggp     /usr/bin/Mail -s "CVS update report: SGGP project" jabril@imim.es gparra@imim.es
#
#
projects    (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.projects
scimit      /usr/bin/Mail -s "CVS update report: SCIMIT"       jabril@imim.es gparra@imim.es
orthologous /usr/bin/Mail -s "CVS update report: ORTHOLOGOUS"  jabril@imim.es gparra@imim.es
chr22       /usr/bin/Mail -s "CVS update report: CHR22"        jabril@imim.es gparra@imim.es
humangenome /usr/bin/Mail -s "CVS update report: HUMAN GENOME" jabril@imim.es gparra@imim.es
#
#
rguigo      (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_rguigo
jabril      (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_jabril
gparra      (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_gparra
scaste      (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_scaste
eblanco     (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_eblanco
xfustero    (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog.users_xfustero
#
#
DEFAULT (echo ""; id; echo %{sVv}; date; cat) >> $CVSROOT/CVSLOGS/commitlog
